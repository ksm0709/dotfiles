# 설계 문서: 서브에이전트 Task 관리 방식 개선 (플러그인 방식)

## 개요 (Overview)

**제안 ID**: `update-subagent-task-management`  
**설계일**: 2026-01-30  
**설계자**: PM (Project Manager)  
**버전**: 2.0 (플러그인 방식으로 전환)

---

## 아키텍처 설계 (Architecture Design)

### 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                      PM (Primary)                           │
│                   (todowrite/todoread)                      │
└──────────────────────┬──────────────────────────────────────┘
                       │ 위임 (Task 호출)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                   Subagent (서브에이전트)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              OpenCode Tool 인터페이스                   │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  tasks_init(agent="{name}", title="{title}")   │  │  │
│  │  │  tasks_list(agent="{name}")                     │  │  │
│  │  │  tasks_complete(agent="{name}", id="{id}")     │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                              │                              │
│                              ▼                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              tasks 플러그인 (내부 동작)                 │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  • 파일 생성/관리 (~/.config/opencode/tasks/{agent}/...)      │  │  │
│  │  │  • 마크다운 형식 처리                           │  │  │
│  │  │  • 상태 관리 및 파싱                            │  │  │
│  │  │  • 자동 정리 및 백업                            │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 플러그인 설계 (Plugin Design)

### 플러그인 위치 및 설치 구조

#### 개발 위치 (프로젝트 내)
```
{project}/opencode/custom-plugins/tasks/
├── package.json          # NPM 패키지 설정
├── tsconfig.json         # TypeScript 설정
├── manifest.json         # OpenCode 플러그인 매니페스트 (Tool 정의)
├── install.sh            # 설치 스크립트
├── src/
│   ├── index.ts          # 메인 진입점 (Tool 핸들러)
│   ├── commands/         # 명령어 구현
│   │   ├── init.ts
│   │   ├── list.ts
│   │   ├── update.ts
│   │   ├── complete.ts
│   │   ├── remove.ts
│   │   ├── status.ts
│   │   └── add-task.ts
│   ├── lib/
│   │   ├── storage.ts    # 파일 저장소
│   │   ├── parser.ts     # 마크다운 파서
│   │   └── formatter.ts  # 출력 포맷터
│   └── types/
│       └── index.ts      # 타입 정의
├── dist/                 # 컴파일된 출력
├── docs/
│   └── tasks-tools-guide.md  # 상세 사용 가이드
└── README.md             # 문서
```

#### 설치 위치 (사용자 홈)
```
~/.config/opencode/
├── plugins/
│   ├── tasks.ts          # 메인 플러그인 파일 (OpenCode가 로드)
│   └── tasks-manifest.json # 매니페스트
├── shared/
│   └── tasks/            # 라이브러리 소스
│       └── src/
├── custom-plugins/
│   └── tasks/
│       └── docs/
│           └── tasks-tools-guide.md
├── tasks/                # 작업 저장소
│   └── {agent-name}/
│       └── {task-title}.md
└── AGENTS.md             # tasks tools guide가 추가됨
```

### OpenCode Tool 인터페이스

Tasks Plugin은 OpenCode Tool로 등록되어 에이전트가 직접 호출할 수 있습니다. 모든 도구는 `~/.config/opencode/plugins/tasks.ts`에 정의되어 있습니다.

#### 1. 작업 목록 생성 (tasks_init)
```
tasks_init(agent="{agent-name}", title="{task-title}", file="{tasks-md-file}")
```
- 새로운 작업 목록 파일 생성
- 마크다운 템플릿으로 초기화

**예시**:
```
tasks_init(
  agent="senior-sw-engineer",
  title="implement-user-auth",
  file="/home/taeho/dotfiles/changes/update-subagent-task-management/tasks.md"
)
```

**반환값**: `✅ Task list "implement-user-auth" initialized successfully for agent "senior-sw-engineer"`

#### 2. 작업 목록 조회 (tasks_list)
```
tasks_list(agent="{agent-name}", format="{json|markdown|table}")
```
- 현재 작업 목록 및 상태 조회
- format 파라미터는 선택사항 (기본값: markdown)

**예시**:
```
tasks_list(agent="senior-sw-engineer", format="markdown")
```

**반환값 예시**:
```markdown
# Task List: implement-user-auth

**에이전트**: senior-sw-engineer  
**생성일**: 2026-01-30 14:30:00  
**세션 ID**: abc-123-def

---

## 작업 목록

- [x] 1. 요구사항 분석
  - 사용자 인증 요구사항 정의
  - 보안 표준 및 규제 준수 확인
- [ ] 2. 기능 구현 (진행 중)
  - [x] 2.1 핵심 로직 개발
    - JWT 토큰 생성 및 검증 구현
    - 비밀번호 해싱 및 검증 로직
  - [ ] 2.2 테스트 코드 작성
    - 단위 테스트 커버리지 80% 이상
    - 통합 테스트 시나리오 작성
- [ ] 3. 검증 및 리팩토링
  - 코드 리뷰 및 품질 검증
  - 성능 최적화

---

## 진행 상황 요약

**현재 단계**: 2. 기능 구현  
**상태**: in_progress  
**완료율**: 40% (2/5)
```

**출력 예시**:
```markdown
# Task List: implement-user-auth

**에이전트**: senior-sw-engineer  
**생성일**: 2026-01-30 14:30:00  
**세션 ID**: abc-123-def

---

## 작업 목록

- [x] 1. 요구사항 분석
  - 사용자 인증 요구사항 정의
  - 보안 표준 및 규제 준수 확인
- [ ] 2. 기능 구현 (진행 중)
  - [x] 2.1 핵심 로직 개발
    - JWT 토큰 생성 및 검증 구현
    - 비밀번호 해싱 및 검증 로직
  - [ ] 2.2 테스트 코드 작성
    - 단위 테스트 커버리지 80% 이상
    - 통합 테스트 시나리오 작성
- [ ] 3. 검증 및 리팩토링
  - 코드 리뷰 및 품질 검증
  - 성능 최적화

---

## 진행 상황 요약

**현재 단계**: 2. 기능 구현  
**상태**: in_progress  
**완료율**: 40% (2/5)
```

#### 3. 작업 상태 업데이트 (tasks_update)
```
tasks_update(agent="{agent-name}", id="{task-id}", status="{pending|in_progress|completed}")
```
- 특정 작업의 상태 변경
- status 값: pending, in_progress, completed

**예시**:
```
tasks_update(agent="senior-sw-engineer", id="task-2.2", status="completed")
```

**반환값**: `✅ Task "task-2.2" updated to "completed" for agent "senior-sw-engineer"`

#### 4. 작업 완료 처리 (tasks_complete)
```
tasks_complete(agent="{agent-name}", id="{task-id}")
```
- 작업 완료 처리 (status를 completed로 설정하는 단축키)

**예시**:
```
tasks_complete(agent="senior-sw-engineer", id="task-2.2")
```

**반환값**: `✅ Task "task-2.2" marked as completed for agent "senior-sw-engineer"`

#### 5. 작업 추가 (tasks_add)
```
tasks_add(agent="{agent-name}", title="{task-title}", details=["{detail1}", "{detail2}"], parent="{parent-id}")
```
- 새로운 작업 항목 추가
- details: 세부 작업 목록 (문자열 배열, 선택사항)
- parent: 부모 작업 ID (하위 작업으로 추가시 선택사항)

**예시**:
```
# 세부사항 없이 추가
tasks_add(agent="senior-sw-engineer", title="에러 처리 구현")

# 세부사항과 함께 추가
tasks_add(
  agent="senior-sw-engineer",
  title="에러 처리 구현",
  details=["예외 케이스 정의", "에러 로깅 구현", "사용자 친화적 메시지"]
)

# 하위 작업으로 추가
tasks_add(
  agent="senior-sw-engineer",
  title="하위 작업",
  parent="task-2"
)
```

**반환값**: `✅ Task "에러 처리 구현" added successfully for agent "senior-sw-engineer"`

#### 6. 작업 제거 (tasks_remove)
```
tasks_remove(agent="{agent-name}", id="{task-id}")
```
- 작업 항목 제거
- Tool로 호출 시 자동으로 확인 없이 제거됨

**예시**:
```
tasks_remove(agent="senior-sw-engineer", id="task-2.3")
```

**반환값**: `✅ Task "task-2.3" removed successfully for agent "senior-sw-engineer"`

#### 7. 진행 상황 요약 (tasks_status)
```
tasks_status(agent="{agent-name}")
```
- 현재 진행 상황 요약 정보 출력
- 완료율, 완료된 작업 수, 전체 작업 수 등 표시

**예시**:
```
tasks_status(agent="senior-sw-engineer")
```

**반환값 예시**:
```
📊 Task Status for senior-sw-engineer

📋 implement-user-auth
Status: in_progress (40% complete)
Completed: 2 / 5 tasks
Current Phase: 2. 기능 구현
```

---

## 에이전트 설정 (Agent Configuration)

에이전트가 Tasks 도구를 사용하려면 frontmatter에 tasks 도구들을 명시해야 합니다.

### frontmatter 설정

에이전트가 Tasks 도구를 사용하려면 frontmatter에 `tasks_*` 와일드카드를 추가하세요.

```yaml
---
description: 에이전트 설명
mode: subagent
tools:
  bash: true
  write: true
  read: true
  # ... 기타 도구들
  tasks_*: true
---
```

**참고**: `tasks_*` 와일드카드는 `tasks_init`, `tasks_list`, `tasks_update`, `tasks_complete`, `tasks_add`, `tasks_remove`, `tasks_status`를 모두 포함합니다.

### 워크플로우에서의 사용
에이전트는 워크플로우 단계에서 다음과 같이 tasks 도구를 호출합니다:

```markdown
### 1. 작업 계획
- **Action**: 작업을 계획하고 초기화합니다.
- **Tool Usage**:
  ```
  tasks_init(agent="senior-sw-engineer", title="API-구현", file="./tasks.md")
  ```

### 2. 작업 상태 업데이트
- **Action**: 현재 작업의 상태를 업데이트합니다.
- **Tool Usage**:
  ```
  tasks_update(agent="senior-sw-engineer", id="task-1", status="in_progress")
  ```

### 3. 작업 완료
- **Action**: 작업을 완료 처리합니다.
- **Tool Usage**:
  ```
  tasks_complete(agent="senior-sw-engineer", id="task-1")
  ```
```

### AGENTS.md 참조
모든 에이전트는 `~/.config/opencode/AGENTS.md`의 "Task Management Tools" 섹션에서 공통된 사용 지침을 참조할 수 있습니다. 이 섹션은 install.sh에 의해 자동으로 업데이트되며, guarding 주석으로 감싸져 있습니다.

---

## 데이터 모델 (Data Model)

### 파일 저장 구조
```
~/.config/opencode/tasks/
├── senior-sw-engineer/
│   ├── implement-user-auth.md
│   └── fix-bug-123.md
├── py-code-reviewer/
│   ├── review-pr-456.md
│   └── review-pr-789.md
└── qa/
    ├── verify-login.md
    └── test-payment.md
```

### 파일 형식 (마크다운)

```markdown
# Task List: {작업 제목}

**에이전트**: {agent-name}  
**생성일**: {YYYY-MM-DD HH:MM:SS}  
**세션 ID**: {session-id}  
**작업 ID**: {task-id}

---

## 작업 목록 (Task List)

- [x] 1. 작업 계획 수립
  - 요구사항 및 범위 정의 완료
  - 리소스 할당 및 일정 수립
  - [x] 1.1 요구사항 분석
    - 기능적/비기능적 요구사항 정리
    - 제약사항 및 가정 확인
  - [x] 1.2 작업 목록 작성
    - 세부 작업 분해 및 우선순위 설정
- [ ] 2. 기능 구현 (진행 중)
  - 핵심 비즈니스 로직 개발
  - 에러 처리 및 예외 케이스 구현
  - [x] 2.1 핵심 로직 개발
    - 알고리즘 구현 및 최적화
    - 데이터 모델 설계
  - [ ] 2.2 테스트 코드 작성
    - 단위 테스트 커버리지 확보
    - 엣지 케이스 테스트 작성
  - [ ] 2.3 문서화
    - API 문서 작성
    - 사용 가이드 작성
- [ ] 3. 검증 및 리팩토링
  - 코드 품질 검증 및 개선
  - 성능 테스트 및 최적화
- [ ] 4. 최종 제출
  - 최종 리뷰 및 승인
  - 배포 준비

---

## 진행 상황 요약 (Progress Summary)

**현재 단계**: 2. 기능 구현  
**상태**: in_progress  
**완료율**: 40% (2/5)  
**메모**: 핵심 로직 개발 완료, 테스트 코드 작성 중

---

## 변경 이력 (Change Log)

| 시간 | 작업 | 상태 |
|------|------|------|
| 2026-01-30 10:00 | 작업 1 완료 | completed |
| 2026-01-30 11:30 | 작업 2-1 완료 | completed |
```

---

### TypeScript 타입 정의

```typescript
// src/types/index.ts

export interface TaskDetail {
  id: string;
  title: string;
  status: 'pending' | 'in_progress' | 'completed';
  details: string[];  // 작업 세부사항 (짧은 문장 리스트)
  subtasks?: TaskDetail[];
  createdAt: string;
  updatedAt: string;
}

export interface TaskList {
  title: string;
  agent: string;
  createdAt: string;
  sessionId: string;
  tasks: TaskDetail[];
  currentPhase?: string;
  memo?: string;
}

export interface TaskStatus {
  agent: string;
  title: string;
  status: 'pending' | 'in_progress' | 'completed';
  completionRate: number;  // 0~100
  completedCount: number;
  totalCount: number;
  currentPhase?: string;
}
```

---

## 에이전트 통합 (Agent Integration)

### 기존 워크플로우 → 플러그인 워크플로우 매핑

| 기존 (todowrite) | 플러그인 방식 |
|------------------|--------------|
| `todowrite`로 초기화 | `tasks init`로 파일 생성 |
| `todowrite`로 상태 업데이트 | `tasks update` 또는 `tasks complete` |
| `todoread`로 상태 확인 | `tasks list` 또는 `tasks status` |
| 상태 변경 (in_progress) | `tasks update --status in_progress` |

### 구현 예시

#### 1. Task List 초기화
```markdown
<!-- 기존 -->
- [ ] `todowrite`로 전체 작업 계획 수립

<!-- 새 방식 (플러그인) -->
- [ ] `tasks init`으로 작업 목록 초기화
  ```bash
  tasks init \
    --agent {agent-name} \
    --title {task-title} \
    --file {tasks-md-file}
  ```
```

#### 2. 상태 업데이트
```markdown
<!-- 기존 -->
- [ ] **현재 단계 상태**: `in_progress`로 설정

<!-- 새 방식 (플러그인) -->
- [ ] **작업 상태 업데이트**:
  ```bash
  tasks update --agent {agent-name} --id {task-id} --status in_progress
  ```
```

#### 3. 상태 확인
```markdown
<!-- 기존 -->
- [ ] `todoread`로 현재 상태 확인

<!-- 새 방식 (플러그인) -->
- [ ] **작업 상태 확인**:
  ```bash
  tasks list --agent {agent-name} --format markdown
  # 또는
  tasks status --agent {agent-name}
  ```
```

#### 4. 작업 완료
```markdown
<!-- 기존 -->
- [ ] **완료된 작업**: `completed`로 상태 변경

<!-- 새 방식 (플러그인) -->
- [ ] **작업 완료 처리**:
  ```bash
  tasks complete --agent {agent-name} --id {task-id}
  ```
```

---

## 트레이드오프 분석 (Trade-off Analysis)

### 장점 (Pros)

1. **인지적 부하 감소**: 에이전트는 복잡한 파일 IO를 신경 쓸 필요 없음
2. **일관성**: 모든 에이전트가 동일한 인터페이스 사용
3. **유지보수성**: 파일 형식 변경 시 플러그인만 수정
4. **확장성**: 새로운 기능을 플러그인에 쉽게 추가
5. **가시성**: 외부에서 파일 내용 직접 확인 가능
6. **추상화**: 복잡한 로직을 플러그인이 처리

### 단점 (Cons)

1. **초기 개발 비용**: 플러그인 개발 필요
2. **의존성**: 에이전트가 플러그인에 의존
3. **디버깅**: 플러그인 문제 발생 시 추적 어려움
4. **학습 곡선**: 새로운 명령어 학습 필요

### 완화 방안 (Mitigations)

1. **명확한 문서화**: 플러그인 사용법을 상세히 문서화
2. **에러 처리**: 플러그인에서 명확한 에러 메시지 제공
3. **폴백 메커니즘**: 플러그인 실패 시 수동 파일 관리 가이드 제공

---

## 구현 가이드라인 (Implementation Guidelines)

### 플러그인 개발 단계

#### Phase 1: 핵심 기능 (MVP)
- [ ] `tasks init` 명령어 구현
- [ ] `tasks list` 명령어 구현
- [ ] `tasks update` 명령어 구현
- [ ] `tasks complete` 명령어 구현
- [ ] 마크다운 파일 생성 및 파싱

#### Phase 2: 확장 기능
- [ ] `tasks add-task` 명령어 구현
- [ ] `tasks status` 명령어 구현
- [ ] `tasks cleanup` 명령어 구현
- [ ] 다양한 출력 포맷 지원 (json, table)

#### Phase 3: 고급 기능
- [ ] 자동 정리 기능
- [ ] 통계 및 리포트 생성
- [ ] 검색 및 필터링
- [ ] 백업 및 복원

### 에이전트 업데이트 단계

#### Step 1: 도구 설정 변경
- [ ] Frontmatter에서 todowrite/todoread 제거
- [ ] `bash` 도구 활성화 (플러그인 호출용)

#### Step 2: 워크플로우 업데이트
- [ ] 모든 todowrite/todoread 참조를 플러그인 명령어로 변경
- [ ] 플러그인 사용 예시 추가

#### Step 3: 가이드라인 업데이트
- [ ] Todo Management 가이드라인을 플러그인 기반으로 변경
- [ ] 플러그인 설치 및 사용법 문서화

---

## 보안 고려사항 (Security Considerations)

1. **파일 접근 권한**:
   - ~/.config/opencode/tasks/ 디렉토리는 적절한 권한 설정 (기본 755)
   - 민감한 정보는 Task 파일에 저장하지 않음

2. **입력 검증**:
   - 플러그인에서 모든 입력값 검증
   - 경로 탐색 공격 방지

3. **파일 생명주기**:
   - ~/.config/opencode/tasks/의 파일은 영구 저장됨
   - 주기적인 정리 필요 (cleanup 명령어 사용)

---

## 테스트 전략 (Testing Strategy)

### 플러그인 단위 테스트
- [ ] 각 명령어 동작 검증
- [ ] 마크다운 파싱 정확성 검증
- [ ] 파일 생성/수정/삭제 동작 검증
- [ ] 에러 처리 검증

### 통합 테스트
- [ ] 실제 서브에이전트 호출 시 플러그인 동작 확인
- [ ] 워크플로우 단계별 상태 업데이트 흐름 검증
- [ ] 다중 작업 동시 처리 검증

### 수동 테스트
- [ ] 각 서브에이전트별 Task 파일 생성 테스트
- [ ] 플러그인 명령어 사용성 테스트
