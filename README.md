# Personal dotfile setup scripts

Setup script with sub-folder name will trigger the `setup.sh` in the sub-folder.
Each sub-folder's setup scripts are responsible for installing app/environment.

```
./setup.sh lazyvim
```
